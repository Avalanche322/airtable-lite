{
  "name": "@apps/server",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "seed": "pnpm -s build && node dist/seed.js",
    "clear-table": "pnpm -s build && node dist/clear-table.js"
  },
  "dependencies": {
    "@types/pg": "^8.15.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.1",
    "@types/ws": "^8.5.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}